{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/assets/(.*)",
      "headers": {
        "Cache-Control": "public, max-age=31536000, immutable"
      }
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "_comment": "Environment variables should be set in Vercel Dashboard instead of here",
  "_instructions": [
    "Go to Vercel Dashboard > Project Settings > Environment Variables",
    "Add: GEMINI_API_KEY = your_api_key_here",
    "Apply to: Production, Preview, Development",
    "Redeploy after adding the variable"
  ],
  "_alternative_env_config": {
    "GEMINI_API_KEY": "@gemini-api-key"
  },
  "_note": "The above env config requires creating a secret first: vercel secrets add gemini-api-key your_actual_key"
}